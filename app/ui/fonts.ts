// Temporarily using system font to avoid Google Fonts fetch during build
export const inter = {
  className: 'font-sans',
  variable: '--font-inter',
};

export const lusitana = {
  className: 'font-sans',
  variable: '--font-inter',
};
